<?xml version="1.0" encoding="UTF-8"?>
<!-- this is the config file for academia-migrate. the environment must be set to 'dev' or 'prod', which impacts the Zenodo API URL (production vs. sandbox) -->
<config>
	<env>dev</env>
</config>
